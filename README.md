# Stock Agent

一个基于大模型和机器学习的智能股票分析助手。

## 安装

你可以通过以下命令在本地安装：

```bash
pip install .
```

## 使用

安装后，你可以直接在命令行运行：

```bash
stock-agent --now
```

或者作为后台进程运行：

```bash
stock-agent
```

## 配置

请确保在运行目录下存在 `config.json` 文件。
